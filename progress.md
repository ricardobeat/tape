# Progress: Duktape C3 — test262 Conformance Tracker

**Last Updated:** Session 261 (B51 full UTF-16 string model migration + B32 libregexp UTF-16 exec wiring; B39 nested destructuring default params; B49 Array length descriptor matrix progress, see Session Log)
**Target:** 100% test262 pass rate on the targeted subset (29,444 executable tests; see plan 040 for the subset definition). Original 80% milestone: ~4 points away.

## Summary (full run, session 254, 2026-07-06)

| Metric | Value |
|---|---|
| Pass + Fail + CE (executable) | 29,444 |
| Total passing | 22,607 |
| **Overall pass rate** | **76.8%** |
| Total failing | 6,214 |
| CE unexpected (parser bugs) | 620 |
| CE expected (`negative: phase: parse`) | 3 |
| Skipped | 12,402 |

Up from the session-252 baseline (75.5%, 22,241 pass, 838 unexpected CE) after
the session-253 B37 closure work and this session's full re-baseline. Gap to 100%
= ~6,800 tests. Cluster breakdown, wave plan, and architecture
blockers: `plans/040-test262-100-percent.md`.

## Per-Phase Status (session 254, full run)

| Phase | Total | Pass | Fail | Skip | CE:expected-parse | CE:expected-runtime | CE:unexpected |
|---|---|---|---|---|---|---|---|
| 0-1: Core VM | 2185 | 827 | 235 | 1096 | 3 | 0 | 24 |
| 1: Calling Convention & Closures | 426 | 217 | 112 | 90 | 0 | 0 | 7 |
| 2: Basic Operators | 1969 | 1202 | 144 | 563 | 0 | 0 | 60 |
| 3: Object System | 7766 | 5766 | 875 | 1082 | 0 | 0 | 43 |
| 4: Error Handling & References | 402 | 208 | 82 | 103 | 0 | 0 | 9 |
| 5: Built-in Constructors | 8615 | 6697 | 1100 | 813 | 0 | 0 | 5 |
| 6: Built-in Prototype Methods | 4713 | 3629 | 652 | 427 | 0 | 0 | 5 |
| 7: Remaining ES5 Features | 1035 | 372 | 126 | 458 | 0 | 0 | 79 |
| 8: ES5 Built-in Objects | 2747 | 1257 | 967 | 522 | 0 | 0 | 1 |
| 11: Arrow Functions & Templates | 465 | 192 | 106 | 158 | 0 | 0 | 9 |
| 12-13: Destructuring & Spread | 19 | 17 | 0 | 2 | 0 | 0 | 0 |
| 14: for-of | 751 | 294 | 212 | 169 | 0 | 0 | 76 |
| 15: Classes | 8520 | 951 | 1088 | 6318 | 0 | 0 | 163 |
| 17-20: Map/Set/Symbol/Promise/WeakMap/WeakSet | 1614 | 981 | 168 | 463 | 0 | 0 | 2 |
| 21: Generators | 619 | 189 | 284 | 138 | 0 | 0 | 8 |

## Test Infrastructure

- **Full suite / single phase**: `python3 scripts/run_test262.py [--phase N] --workers 4` — the single canonical runner (~6–8 min full, <1 min per phase). Kills workers over 2 GB RSS (MEMKILL) so runaway tests can't freeze the machine.
- **Per-test results for clustering**: add `--log out/test262_results.tsv`, then e.g. `awk -F'\t' '$1=="FAIL"{print $2}' out/test262_results.tsv | xargs -n1 dirname | sort | uniq -c | sort -rn`
- **Single-test repro with harness + error output**: `bash test262_runner/run_single_test.sh <path-under-test262/test>` (`--keep` emits the combined file for `just lldb` / `--trace-vm`)
- **Quick smoke**: `bash test262_runner/quick.sh` · **Phase counts**: `bash scripts/count_test262_by_phase.sh`
- **Delta between runs**: `bash scripts/test262_delta.sh`
- **Build**: `c3c build batch_test_vm` or `c3c build test_vm`
- (Removed in session 250: `phase_runner.py`, `capture_fails.py`, `analyze_phase3_failures.py` — all superseded by `run_test262.py --log`; they carried stale third copies of the skip list.)

## Session Log (condensed, newest first, last 10 sessions)

| Session | Summary | test262 impact |
|---|---|---|
| 259/261 | **B51 (full UTF-16 code-unit string model migration) + B32 (libregexp UTF-16 exec wiring)** (src/hstring.c3, src/lexer.c3, src/heap.c3, src/compiler/constants.c3, src/builtins/{core,global,symbol,iterator,array,string,regexp}.c3, libregexp/re_wrapper.c, BACKLOG.md, progress.md). Migrated the string model from UTF-8/codepoint-indexed storage to CESU-8 (each UTF-16 code unit, including surrogate halves, independently encoded as its own standalone UTF-8 sequence) — matches real Duktape's internal representation (verified against vendored `duktape/src-separate/duk_unicode_support.c`). Landed in 5 tracked stages, each built + rosetta 100/100 + test262-phase-gated + committed separately (`05bf848`, `15651d0`, `f7e0903`). **Stage 1+2**: encode/decode primitives; `Heap.str_intern` established as the single normalization chokepoint (two other hand-rolled interning paths bypassed it and broke `===`/property-lookup for literal-vs-`\u{}`-escape spellings of the same astral string — fixed); `charCodeAt`/`codePointAt`/`.at()`/`fromCodePoint` surrogate semantics; string iterator/`split("")`/`Array.from(string)` now combine surrogate pairs into one iteration step; `print()` output re-encodes CESU-8 back to standard UTF-8. **Stage 3**: `charAt`/`slice`/`substring`/`substr`/`indexOf`/`lastIndexOf`/`includes`/`startsWith`/`endsWith`/`padStart`/`padEnd` rewritten on code-unit semantics; fixed an unrelated latent 256-byte truncation bug in `slice`/`substring`. **Stage 4**: JSON escape round-trip audited, no changes needed. **Stage 5 (B32)**: `re_wrapper.c`'s `re_exec` converts the CESU-8 subject to a flat UTF-16 buffer and calls `lre_exec` with `cbuf_type=1` (libregexp internally promotes to `cbuf_type=2` — surrogate-pair combining — whenever the compiled bytecode has the `u`/`v` flag; passing `cbuf_type=2` directly from the wrapper is wrong because it doubles `cbuf_end`'s stride against a buffer that's always 2 bytes/unit). Capture offsets map back from UTF-16 code-unit indices to CESU-8 byte offsets via a per-call offset table. Surfaced and fixed two related pre-existing bugs: `AdvanceStringIndex` on a zero-length global match advanced by a fixed 1 byte instead of one code unit / full surrogate pair (new `hstring::advance_string_index_byte_len`, applied at all 6 empty-match-advance sites in `regexp.c3`/`string.c3`); the replace/replaceAll loops silently dropped skipped source characters when advancing past an empty match (`"ab".replace(/x*/g,"-")` gave `"-b-"` instead of `"-a-b-"`). Phase 8 (ES5 Built-ins): 1257 → 1275 pass. Ad hoc RegExp/string-regex-method sweep: 876 → 878 pass, 1 pre-existing timeout eliminated. Known residual gaps (documented, not fixed): raw astral UTF-8 typed literally in source bypasses lexer's fast-path normalization; `String.prototype.normalize()` doesn't recombine surrogate pairs before libunicode; named capture groups (`.groups`) return `undefined` regardless of astral content (confirmed pre-existing on plain ASCII, unrelated). Filed **B55** (native UTF-16/Latin1 storage, avoiding CESU-8's per-regex-exec conversion cost and byte overhead) as an explicit follow-up rather than silently accepted debt. | Phase 8: 1257→1275 pass; RegExp sweep: 876→878 pass |
| 261 | **B39 nested destructuring default parameters + B49 property-descriptor matrix progress** (`src/compiler/functions.c3`, `src/builtins/object.c3`, `src/builtins/array.c3`, `src/vm/vm_objects.c3`, `BACKLOG.md`, `progress.md`). **B39**: `collect_obj_param_binds`/`collect_arr_param_binds` now mark nested patterns as synthetic so a trailing `= default` binds to the synthetic group; destructuring loops in `compile_inner_function`, `compile_arrow_inner_reparse`, and `parse_function_body` now evaluate the default thunk when the synthetic group is undefined before `REQUIRE_OBJ`. Closes `language/statements/function/dstr/ary-ptrn-elem-ary-elem-init.js` and related surfaces. **B49**: Array-exotic `.length` created by `SETALEN` and `Array.of` now uses `PROP_FLAGS_WENC` (writable, non-enumerable, non-configurable); `Object.defineProperty` array-length path rejects `{enumerable:true}` and converts descriptor `value` through `builtin_to_number_vm` so objects coerce via ToPrimitive. Closes `15.2.3.6-4-118`/119/121/125/146-151. Local: rosetta 100/100; phase 3 rerun 5857 pass / 789 fail / 38 CE:unexpected (+19 pass, −19 fail vs session-254 baseline). | Phase 3: +19 pass, −19 fail; rosetta 100/100 |
| 260 | **B47 partial — Function.prototype.toString source-text retention** (src/builtins/function.c3, src/bytecode.c3, src/compiler/{class,expressions,functions,statements}.c3, src/lexer.c3, test/tostring_check.js, BACKLOG.md, progress.md). Implemented ES2019 §19.2.3.5 source-text retention for compiled JS functions. **(1) Storage**: `CompiledFunction` gained `src_ptr`/`src_len` (`src/bytecode.c3`) — heap-owned copy of the original source span; `libc::malloc`'d at compile time, freed when the function object is GC'd. **(2) Capture points**: every `compile_inner_function` / `function_expr` / `function_declaration` / `function_expr_body` / arrow-parser / hoist / export-default / export-named site now captures `lexer.token_start` of the opening token (`function`, `[`, arrow head, or computed-key bracket) before consuming subsequent tokens clobbers it; `compile_arrow_inner{,_reparse}` additionally captures `body_end_pos` right after the body's closing token and walks back over trailing whitespace + line/block comments so the slice is exact. **`refresh_token_start()`** added to `Lexer` (`src/lexer.c3`) so class-body / object-literal loops can remember the offset of the *first* token of each iteration (whose `token_start` may be stale from the previous iteration's closing `}`). Lexer also gained `token_start` / `lookahead_token_start` fields with peek/restore semantics so `lookahead_token_start` keeps `token_start` accurate after `peek()`/`advance()` pairs. **(3) Async prefix**: `compile_inner_function` prepends `"async "` to the retained source for `async` functions (since the `async` keyword was consumed before `function` during hoisting). **(4) Dispatch**: `builtin_function_proto_toString` (`src/builtins/function.c3`) now returns the retained source via `builtin_intern_string` when the function is a compiled user script (`comp_func != null && builtin_fn_index < 0`), falling back to the `[native code]` placeholder otherwise. Test: `test/tostring_check.js` confirms all six patterns (`function fd`, `var fe = function`, `var fn = function named`, three arrow forms) return their original source. **Phase 5: 6680 → 6697 pass (+17), 1117 → 1100 fail (−17).** 26 `built-ins/Function/prototype/toString/*` tests now pass (function-declaration, function-expression, named function-expression, method-object, method-class-expression, method-class-statement, getter-object, getter-class-expression, getter-class-statement, setter-object, setter-class-expression, setter-class-statement, generator-function-declaration, generator-function-expression, line-terminator-normalisation-{LF,CR,CR-LF}, S15.3.4.2_A{6,8..14}). Remaining 25 fail in two clusters: **(a) comment preservation** — `arrow-function.js`, `async-arrow-function.js`, `async-function-expression.js`, `async-function-declaration.js`, etc. all expect source with embedded `/* */` and `//` comments preserved (24 tests; requires lexer to retain comment spans, an architectural change). **(b) static methods, computed-key methods, and bound-function tests** (1 test) — `static`/`*`/computed-key offset tracking requires a small additional fix in the class-body loop. Rosetta 100/100. No regressions. |
| 259 | **Unicode identifier tables via libregexp + lexer buffer bump** (libregexp/unicode_wrapper.{c,h}, src/lexer.c3, src/unicode_bindings.c3, BACKLOG.md, progress.md). Closed B52 plus an unmarked sub-bug in the slow escape-decode path. `(1)` Tables: `is_identifier_start(uint)` / `is_identifier_part(uint)` in `src/lexer.c3` rewritten as `@inline` thin wrappers around new `unicode_bindings::unicode_is_identifier_start(uint)` / `unicode_is_identifier_continue(uint)` C externs. C side (`libregexp/unicode_wrapper.c`) fast-paths ASCII (`[A-Za-z_$]` for start, plus `0-9` for continue); everything ≥ 128 routes through `lre_is_id_start` / `lre_is_id_continue`, which consult libregexp's full ID_Start / ID_Continue property tables (same source QuickJS uses for `lre_js_is_ident_first/next`). ZWNJ (U+200C) and ZWJ (U+200D) added explicitly on the continue side per UAX #31 (libregexp's tables don't include them; QuickJS adds them in the same place). `(2)` Buffer: `Lexer.ident_buf` bumped from `char[256]` with `buf_len < 250` to `char[4096]` with `buf_len < 4090`, so long escaped-identifier names (e.g. `var _\u{...83 codepoints}` ≈ 270 UTF-8 bytes) no longer truncate silently. `(3)` Scope: full Other_ID_Continue grandfathered chars (U+00B7 ·, U+0387 ·, U+1369-1371 Ethiopic digits, U+19DA ᧚) + all astral ID_Continue planes (U+11A01+, U+11D31+) now parse as identifier continuations in both fast and escape-decode paths. Phase 0-1: 798 → 827 pass (+29), 263 → 235 fail (-28), 25 → 24 CE:unexpected (-1). Local tests: rosetta 100/100, destructure_regression 90/90, destructure_array 19/19, rest_nested 9/9, regexp 15/15. Remaining `language/identifiers/start-unicode-*.js` failures are now **all** register-limit exceedances (8,327 `var` statements in one scope > 511 reg limit) — unrelated to identifier classification, need a register allocator rework. | Phase 0-1: +29 pass, -28 fail, -1 CE:unexpected |
| 258 | **Array/Function.prototype.length + exotic ARRAY/ARGUMENTS delete/hasOwnProperty/concat** (src/builtins/{function,object,array}.c3, src/hobject.c3, BACKLOG.md, progress.md). Two distinct wins on B46/plan-022 surfaces. **(1) Array.prototype.length / Function.prototype.length descriptors**: both built-in prototypes were missing their required `length: 0` own property per spec — Function.prototype per ES5 §15.3.5.1 (`{writable:false, enumerable:false, configurable:true}`), Array.prototype per ES2015 §22.1.3 (`{writable:true, enumerable:false, configurable:false}`). `register_function_constructor` now installs `length: 0` on Function.prototype with `PROP_FLAGS_NWC`. For Array.prototype, `array_size` is 0 but no descriptor existed — `builtin_object_getOwnPropertyDescriptor` now synthesises the descriptor directly from `array_size` for any ARRAY exotic, `hasOwnProperty` returns `true`, `propertyIsEnumerable` returns `false`, and `HObject.es_delete_prop` returns `false` so the strict-mode TypeError fires (per ES §13.5.1.2 [[Delete]] step 5: non-configurable → false). Closes `built-ins/Function/prototype/length.js` and `built-ins/Array/prototype/length.js`. **(2) B46 exotics** (committed in two halves): `es_delete_prop` for ARRAY/ARGUMENTS now clears the dense-part slot when a numeric index falls in `0..array_size-1` (previously silent no-op); `Object.prototype.hasOwnProperty`/`propertyIsEnumerable` consult the dense part for those classes (so `a.hasOwnProperty("0")` works after `concat`/`slice`); `Array.prototype.concat` refactored to share `concat_process_item` between `this` and each arg so non-array `this` becomes a single element per ES2022 §23.1.3.1 step 5. `array_species_create_result` returns a plain `Array` when `original` is not itself an Array, per §9.4.2.3 step 3-4. Phase 5: 6650 → 6680 pass (+30), Phase 6: 3598 → 3629 pass (+31) across the two commits of work. Rosetta 100/100, local destructuring tests unchanged. | Phase 5 +30, Phase 6 +31 |
| 257 | **ES2015 shorthand object properties + ES2022 Error cause option-bag** (src/compiler/expressions.c3, src/builtins/error.c3, progress.md, BACKLOG.md). **Shorthand object props** (`{ cause }` ≡ `{ cause: cause }`, per ES2015 §12.2.6.8): the object-literal parser in `CompilerContext.object_literal` previously fell through to the `key: value` arm when the next token wasn't `(` or `:`, returning a parse error on `{ cause }`. Added a third `else if` arm for `is_shorthand_key && (COMMA or RBRACE)` that emits a `GETVAR` of the key's name index into a fresh register and `PUTPROP`s it as usual. Tracked `is_shorthand_key` (true iff the key came from an identifier/string/keyword) and `shorthand_name_idx` (the constant-pool index of the key string) at the per-iteration scope so the shorthand arm can find the name without re-parsing. `{ cause, x, y: 1, foo() {} }` all work together; method shorthand and `*method` are unaffected. **ES2022 Error cause option-bag** (§20.5.8.1 InstallErrorCause): `builtin_error_constructor_shared` in `src/builtins/error.c3` now reads a 2nd `options` argument after `.message` and, if it's an Object with an own `cause` property, installs the cause as a non-enumerable writable+configurable data property via `put_prop(..., PROP_FLAGS_WC)`. Mirrored the same option-bag install into `builtin_aggregate_error` for its 3rd arg (after `errors` and `message`). Phase 4: 208→212 pass (+4), 82→86 fail (+4 — the 4 newly-`PASS` tests in single-test mode still `FAIL` in the batch worker due to a pre-existing `this`-binding bug for `__hasOwnProperty(obj, name)`-style calls that drops `this`; tracked separately), 9→1 CE (−8, all 8 `try/dstr/...rest-*-*.js` tests shifted to `FAIL` runtime because they exercise the long-tail nested-destructuring paths, plus both `cause_property` CEs closed). Local tests: rosetta 100/100, destructure_regression 90/90, destructure_array 19/19, destructure_object 18/18. Cause tests now `PASS`: `built-ins/Error/cause_property.js`, `built-ins/NativeErrors/cause_property_native_error.js`, `built-ins/AggregateError/cause-property.js`. | Phase 4: 212 pass (+4), 1 CE (−8) |
| 256 | **Rest-with-nested in bare for-of and catch paths + multi-type property keys** (src/compiler/statements.c3, progress.md). Continued the previous session's work on rest-with-nested destructuring, extending the same parser/emission fix from the var/let/const for-of path to two more inline destructuring parsers: bare `for ([pattern] of x)` and `catch ([pattern])`. Added three-way ELLIPSIS dispatch (`[`, `{`, simple identifier), marker bind for the rest position (`is_rest=true, is_nested=true, name_idx=0`), shared rest-collection code path with marker detection, and second inner iterator (array nests) / GETPROP (object nests) for sub-destructure. Also widened property-key parsing from `expect(IDENTIFIER)` to multi-type (IDENTIFIER, STRING, NUMBER, keyword) in three more sites (bare for-of object-pattern, catch array-pattern, catch object-pattern), matching the var/let/const fix from the previous session. Removed the `is_rest` skip from the `has_array_nested` check in all three paths so `[[...x]]` is correctly recognized as array-nested (pre-existing bug). Local tests: 90/90 destructure_regression, 31/31 forof_destruct, 100/100 rosetta, 9/9 rest_nested. Phase 4 unchanged (208 pass, 9 CE), Phase 14 unchanged (294 pass, 76 CE). The 76 remaining CEs in phase 14 are pre-existing failures: ~30 evaluation-order side-effect tests, the ~20 generator-rest crash (`[...x]` on generator values), and `[[...x]]`/member-target rest (`for ([...obj.x] of ...)`) patterns not covered by this session. | Phase 14: 294 pass (unchanged); Phase 4: 208 pass (unchanged) |
| 255 | **Nested destructuring in bare for-of and catch-clause parsers** (src/compiler/statements.c3, progress.md). Extended `emit_bare_forof_destruct_loop` and both `compile_catch_array_pattern`/`compile_catch_object_pattern` to handle nested array (`[{x}]`, `[[x,y]]`) and nested object (`{prop: [x,y]}`, `{prop: {x,y}}`) destructuring patterns. Phase 14 CE: 177→76 (−101, +72 pass, +29 FAIL); Phase 4 CE: 32→9 (−23, +13 pass, +10 FAIL). Remaining CEs: rest-with-nested (`[...[x]]`, `[...{x}]`), evaluation-order tests. | Phase 14: 294 pass (+72), 76 CE (−101); Phase 4: 208 pass (+13), 9 CE (−23) |
| 253 | **Closed B37 destructuring surfaces + nested-object-default follow-up; B39c investigated and found spec-correct** (src/compiler/destructuring.c3, src/compiler/context.c3, BACKLOG.md, progress.md). Completed the remaining B37 work from the previous session: nested object patterns with a whole-object default inside array destructuring (`[{x, y} = default]`) now parse and execute in both declaration and assignment forms, including per-property defaults inside the nested object (`[{x = 1} = {}]`). Added `ArrayBind.has_obj_default`/`obj_default_val_reg` to keep whole-object defaults separate from per-property defaults, and reordered parsing so the closing `}` is consumed before reading the `= default` initializer. **B39c** (`function f(a = b, b = 1) {}` TDZ) verified to already match ES2015 §9.2.13 — `f(5)` returns `[5, 1]`, `f(undefined, 5)` and `f()` throw ReferenceError because `b` is in TDZ during `a`'s default evaluation. No code change needed; BACKLOG.md updated. Regressions: rosetta 100/100, destructuring local tests 90/90, phase 4 unchanged, phase 14 +1 pass / −1 fail. Full suite not re-baselined this session. | Phase 14: 222→223 pass, 183→182 fail; phase 4 unchanged |
| 252 | **Closed 5 backlog items from the plan-040 parser-holes wave: B41, B43, B44, B42, B45** (compiler/{expressions,functions,tokens,statements}.c3, BACKLOG.md, plans/040-test262-100-percent.md, scripts/run_test262.py, progress.md). **B41** (verified only — already fixed by the prior session's `40c0ece` generator/finally commit): `continue` in `finally` overriding a `break` no longer hangs. **B43** (verified only — already fixed by the generators branch merge): generator method shorthand (`class C { *m(){} }`, `({ *g(){} })`) already parses and runs; phase 15 CE 930→203 in the first re-measurement. **B44** (implemented): async arrow functions (`async () => 42`, `async x => x`, rest/destructured params, `await` in body). Two-part fix: `is_async_arrow_lookahead()` lookahead in tokens.c3 detects the async-arrow head and recurses into `primary_expr()`; separately, `compile_arrow_inner`/`compile_arrow_inner_reparse` in functions.c3 never propagated `is_async` into the arrow's fresh sub-context, so even correctly-parsed async arrows ran synchronously with no Promise wrapping until fixed. **B42** (implemented): reserved words as object-literal property names/getter-setter names (`{ default: 1, get if(){} }`) — class method names already handled this via `lexer::keyword_to_string`, only the object-literal key parser was strict; widened two call sites in expressions.c3 to match the existing class.c3 pattern. **B45** (implemented, smaller than estimated): for-of/for-in heads with a member-expression LHS (`for (obj.prop of arr)`) plus a related pre-existing gap, bare (no `var`/`let`/`const`) identifier for-in (`for (x in obj)`). New `ForLhsSnapshot` (full lexer+pushback state, not a byte offset — `self.lexer.pos` can already be past a pending lookahead token) lets the member LHS be re-parsed and recompiled fresh every loop iteration via new `emit_forof_loop_member`/`emit_forin_loop_member`/`emit_member_lhs_store` helpers, giving correct per-iteration re-evaluation. Real footprint ~4 tests, not the 150–300 originally estimated — phase 14's 320 CEs are almost entirely B37 destructuring patterns (confirmed by grepping the CE log), untouched by this fix. **Infra**: `run_test262.py` was silently running a stale `batch_test_vm` binary all session (only rebuilt when the binary was *missing*, never on staleness) — understated the first round of B42/B44 gains until caught; now always rebuilds before running. Full fresh-binary run: 71.8%→**75.5%** (22,241 pass, up from 21,121–21,154; unexpected CE 2,035→838). No local test regressions. | 71.8%→75.5%; unexpected CE 2,035→838 |
| 250 | **Full project review — roadmap to 100%, runner consolidation, memory-bomb root cause** (plans/040-test262-100-percent.md, BACKLOG.md, progress.md, plans/index.md, README.md, scripts/run_test262.py, test262_runner/run_single_test.sh; deleted scripts/{phase_runner,capture_fails,analyze_phase3_failures}.py). Two full runs re-baselined the suite at **71.8%** (21,121–21,154 / 29,459 executable; ±33 flakiness → B50). Runner gained `--log FILE` (per-test RESULT⇥path lines for cluster analysis) and a 2 GB per-worker RSS guard. **Memory-bomb root cause found**: huge-length array-like tests (e.g. `findLast/maximum-index.js`, `{length: Number.MAX_VALUE}`) loop near-infinitely due to missing ToLength/2^53 handling, interning a numeric-index key string per iteration — ~300 MB/s per worker × 4 workers until the 10s timeout = the 30GB+ machine freezes (B40). Verified parser holes by repro: reserved words as property names (B42), generator method shorthand in classes/object literals (B43 — the real face of B38, ~1,100 tests), async arrows (B44), for-of member-expression LHS (B45), catch-param destructuring (B37). Verified runtime clusters: Array spec-op ordering/holes/species (B46, 1,323 fails), descriptor validation matrix (B49, ~490), Function.prototype.toString source retention (B47), Number formatting (B48), `continue`-in-`finally`-overrides-`break` infinite loop (B41, 12 timeouts). Flagged architecture blockers: UTF-8-codepoint string model vs UTF-16 spec semantics (B51 — prerequisite for B32), function source-text retention, peephole A/B guardrail, shared spec-op helpers, skip-list duplication. Full wave plan with estimates in plan 040. | re-baseline: 71.8%, roadmap to 100% |
| 249 | **B31 swapped libregexp.c for quickjs-ng** (libregexp/{cutils,libregexp,libregexp-opcode,libunicode,libunicode-table,h-comments-replaced-by-quickjs-ng-vendor}, libregexp/re_wrapper.{c,h}, src/builtins/regexp.c3, src/builtins/string.c3, src/vm/vm_objects.c3, src/re_bindings.c3, scripts/run_test262.py, scripts/test262_skip.cfg, BACKLOG.md, progress.md). Replaced the original-vendor QuickJS libregexp.c (unmaintained) with quickjs-ng's active fork. Brings ES2018-ES2024 features the vendored copy lacked: `\u{1F600}` brace-codepoint escapes, `\p{Letter}` Unicode property escapes, duplicate-named groups across alternatives, full `v`-flag string-set notation, and the per-capture `d`-flag `match.indices` runtime. Wrapper (`re_wrapper.{c,h}`) now maps `RE_FLAG_UNICODE`/`STICKY`/`INDICES`/`UNICODE_SETS` through to `lre_compile` and enforces the u+v mutual exclusion ES2024 added. VM-side flag parsing in RegExp ctor, regex-literal opcode, and create_regexp_from_string helper all surface u/y/d/v through. Test262 skip-list: removed `regexp-unicode-property-escapes`, `regexp-v-flag`, `regexp-duplicate-named-groups` (780 newly-attempted tests now visible in phase 8 alone). Rosetta 100/100. Full test262 rerun: 20,694 pass (+29 vs. session 248's 20,665) on an executable denominator of 30,727 (584 more tests attempted since the skip list is now larger surface). Phase 8: 1197 → 1206 pass (+9). The swap is mostly churn — visibly failing tests are tracked as B32 (multibyte UTF-8 input needs `cbuf_type=2`), B33 (d-flag indices runtime + `hasIndices`/`unicodeSets` accessors not wired), B34 (`String.match` drops `.groups` on non-global named captures). | +29 pass; 584 more attempted |
| 248 | **B30 Array.prototype.pop use-after-free + B28/B25 rosetta fixes + full test262 rerun** (src/builtins/array.c3, src/compiler/context.c3, src/lexer.c3, quickjs/dtoa.c, src/dtoa_wrapper.{h,c}, src/dtoa_bindings.c3, BACKLOG.md, test/rosetta/FAILURES.md, progress.md). Three rosetta bugs closed. **(B30)** `Array.prototype.pop` returned a TVal pointing at the popped HString, then `array_delete_elem` decref'd the slot — if the popped value's only remaining reference was the slot being removed (common with interned strings shared by other array slots, e.g. `push("a"); push("a")`), refcount hit 0, the HString was removed from the string table and freed, and the next allocation (the `out += popped` concat) reused that memory. The reused bytes read back as `""` and the add threw `Cannot convert a Symbol value to a string` on the next iteration. Fix: `incref` the popped element before clearing the slot, transferring ownership to the caller's result register. **(B28)** Peephole fused-compare bridge fix was firing indiscriminately for any `if_false/if_true` at the destination, including the `if_true` *back edge* of a `while (cond1 && cond2)` loop. The structural difference: forward continuations vs loop back edges. Fix: only treat the destination as a bridge when its offset is positive (forward) — preserve the back-edge semantics. Closes heap_sort, shell_sort, range_expansion.compressRange, and merge_sort. **(B25)** `8.3 - 1 === 7.3` is `false` because IEEE 754 represents the two values as different bit patterns (`0x401d333333333334` vs `0x401d333333333333`). This is true in V8, Node, SpiderMonkey, JSC, QuickJS, and our engine — not engine-specific. Updated rosetta test to use approximate equality. Also integrated QuickJS `js_atod` (David-Gay-style parser) via `quickjs/dtoa.c` for future-proofing the decimal-literal path. Full test262 rerun: 20,665 pass (+2,769 vs. session 240's 17,896). Rosetta 100/100. The pass-rate denominator also grew (executable 21,499 → 30,143) because session-240 baseline excluded some phases that the current run includes — absolute pass count is the more honest measure of progress. | +2,769 pass; rosetta 100/100 |
| 242 | **Optional chaining this-binding fixes + parenthesized expression propagation** (compiler/expressions.c3, compiler/context.c3, progress.md). **(1)** Added `OPT_CHAIN` to DOT/LBRACKET handlers in `call_expr` — fixes this-binding for `a.b?.()` and `foo().b?.()` patterns where the DOT is processed in `call_expr` (not `member_expr`). **(2)** Modified `member_expr` DOT/LBRACKET handlers (both RHS and lhs/del modes) to always save `call_prop_obj_reg` for potential this-binding, even when the next token is not a call — enables propagation through parenthesized expressions. **(3)** Added `inside_parens` flag to `CompilerContext` — prevents `call_expr` start check from prematurely freeing `call_prop_obj_reg` when the `member_expr` result is wrapped in grouping parens. **(4)** Modified the `primary_expr` LPAREN handler to propagate `call_prop_obj_reg` through `(expr)`: after consuming `)`, if the next token is LPAREN or OPT_CHAIN, the member reference is kept alive for the outer call; otherwise it is freed. Fixes `(a.b)()` and `(a.b)?.()` this-binding. **(5)** Fixed comma operator to clear `call_prop_obj_reg` after each operand (including the rightmost) — comma expressions always return a value, never a Reference. Fixes `(a.b, c)()` correctly getting `this = undefined`. 15/17 optional-chaining tests pass (2 async failures pre-existing). Phase 11: 141→142 pass, 70→69 fail (+1 from `optional-call-preserves-this.js`). Rosetta 44/44. | Phase 11 +1 pass, −1 fail |
| 241 | **Optional chaining implementation + test262 skip-list regex bug fix** (compiler/expressions.c3, compiler/context.c3, scripts/run_test262.py, scripts/capture_fails.py). **(1)** Implemented ES2020 optional chaining (`?.`) in the compiler with null/undefined short-circuit semantics using deferred jump patching. Supports three access forms: `?.()`, `?.[]`, `?.prop`. Results are non-referenceable — `a?.b = 33`, `a?.b++`, `--a?.b` correctly rejected as SyntaxErrors via `last_was_optional_chain` flag. `this` preservation fixed across optional chains by keeping `call_prop_obj_reg` alive through `?.prop` steps and in `member_expr`. `callee_is_eval = false` for `?.()` ensures indirect eval semantics. 15 of 38 optional-chaining tests pass; remaining 23 fall into CE (negative parse tests), unsupported features (class/async/super), or pre-existing VM bugs (indirect eval). **(2)** Fixed a long-standing regex bug in test262 skip lists where trailing `|` created an empty alternative matching any `features:` header, silently skipping ~200 tests. Removed `optional-chaining` from UNSUPPORTED_PATTERN; added `language/expressions/optional-chaining` to Phase 11. Phase 11: 74→141 pass (+67), 23→70 fail, 325→158 skip (-167), 5→96 CE. Phase 0-1: 682→786 pass (+104). Rosetta 44/44. | Phase 11 +67 pass; Phase 0-1 +104 |
