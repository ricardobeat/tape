# Progress: Duktape C3 — test262 Conformance Tracker

**Last Updated:** Session 100 (function .name + .length flags)
**Target:** 80% test262 pass rate on ES5/ES6 core

## Summary (fresh run, 2026-06-02)

| Metric | Value |
|---|---|
| Total test262 tests | 53,568 |
| Tests run (phases 0-14) | ~42,400 |
| Skipped (unsupported features) | ~20,400 |
| Currently passing | ~7,150 |
| Currently failing | ~14,450 |
| Pass rate (of run) | ~33% |

Fresh counts are generated by `scripts/count_test262_by_phase.sh`. Pass/fail counts
by `python3 scripts/run_test262.py --phase N`. Update after every session.

## Benchmark Summary

See `benchmarks/results.txt`. C3 vs Duktape v2.7.0 ratios (lower is better):
array 1.9x, loop 0.8x, object 0.7x, property_lookup 0.7x, string 0.8x.
Regressions: recursion 1.9x, ic_monomorphic 2.0x, valstack_copy 2.3x.
QuickJS is 3-10x faster on most benchmarks.

## Per-Phase Status (fresh baseline, 2026-06-02)

| Phase | Total | Pass | Fail | Skip | Notes |
|---|---|---|---|---|---|
| 0-1: Core VM | 2,185 | 458 | 430 | 1,297 | VM, lexer, compiler ✅ |
| 1: Calling Convention | 426 | 12 | 76 | 338 | Most skipped (noStrict) |
| 2: Basic Operators | 1,969 | 465 | 679 | 825 | BigInt/gen skipped |
| 3: Object System | 7,766 | 1,949 | 3,850 | 1,967 | Descriptors weak |
| 4: Error Handling | 402 | 63 | 138 | 201 | Error names weak |
| 5: Built-in Constructors | 8,615 | 2,389 | 4,701 | 1,525 | Prop-desc missing |
| 6: Prototype Methods | 4,713 | 937 | 2,840 | 936 | Prop-desc + args |
| 7: ES5 Features | 1,240 | 134 | 159 | 947 | Eval/switch gaps |
| 8: ES5 Built-in Objects | 2,747 | 493 | 791 | 1,463 | Date/RegExp gaps |
| 11: Arrow/Templates | 427 | 59 | 44 | 324 | Working, edge cases |
| 12-13: Destructuring | 19 | 0 | 0 | 19 | All skipped |
| 14: for-of | 751 | 3 | 29 | 719 | All skipped (no iter) |
| 15: Classes | 8,520 | 26 | 236 | 8,258 | Mostly skipped |
| 17-20: Map/Set/Symbol/Promise | 1,588 | 162 | 478 | 948 | Prop-desc gaps |
| 21: Generators | 619 | 0 | 2 | 617 | All skipped (no impl) |

---

## Implemented Features (what works)

### Phase 0-1: Core VM ✅
TVal tagged values, heap allocator, string interning, bytecode encoding,
lexer, compiler, VM execution loop, print/console.log.

### Phase 1: Calling Convention ✅
Register-based call convention, PUTVAR, CLOSURE with env capture, nested calls.

### Phase 2: Basic Operators ✅
All arithmetic, bitwise, logical, comparison, equality operators.
NaN/Infinity/undefined semantics, typeof, comma, void, conditional.
ToNumber coercion for non-numeric types (Session 98).

### Phase 3: Object System ✅
Object/array literals, prototype chain, GETPROP/PUTPROP, numeric keys,
`new` operator, member LHS assignment, array .length.

### Phase 4: Error Handling ✅
Error constructors, try/catch/throw/finally, catchers with activation unwinding.

### Phase 5: Built-in Constructors ✅
Boolean, String, Number, Object, Array, Function constructors.
Number static props/methods, Array.of, Array.prototype.find/findIndex,
Array.prototype.toReversed/toSorted/toSpliced/with.

### Phase 6: Prototype Methods ✅
Math methods, String.prototype, Array.prototype, Number.prototype,
Boolean.prototype, Function.prototype core methods.

### Phase 7: ES5 Features ✅
for-in, instanceof, delete, in operator, switch/case, labeled break/continue,
with statement, eval.

### Phase 8: ES5 Built-in Objects ✅
JSON.parse/stringify, Date (constructors, getters, setters, UTC, toISOString),
RegExp (libregexp), Object.keys/getPrototypeOf/getOwnPropertyDescriptor/
isExtensible/preventExtensions/getOwnPropertyNames/defineProperty/is/hasOwn/setPrototypeOf.

### Phase 9 (Strict Mode) ✅
"use strict" directive, is_strict flag, restricted names, FutureReservedWords,
duplicate property check, with-statement rejection.

### Phase 10 (let/const) ✅
PUSH_LEX/POP_LEX/PUTLEX opcodes, lex_env chain, TDZ enforcement,
const re-assignment check, is_captured shadowing.

### Phases 11-21: ES6+ ✅ (core)
Arrow functions, template literals, tagged templates, nested templates.
Default/rest parameters, spread operator, destructuring (basic).
for-of (no iterator protocol), classes (core: methods, extends, super,
new.target, computed props, getters/setters, no private fields).
Map, Set, WeakMap, WeakSet, Symbol, Promise (no microtask), Generators.

---

## Root Cause Analysis: Top Failure Categories

### 1. Function `.name` not set on JS functions (~2000+ tests)
**Impact**: Every test checking `fn.name` or property descriptors on function objects.
**Status**: ✅ FIXED — CLOSURE opcode now creates `.name` property from template name.
Anonymous functions get `""` per ES6 spec. `.length` flags also fixed to `PROP_FLAGS_NWC`
(non-writable, non-enumerable, configurable) per ES5 §15.3.5.1.

### 2. Lightfunc constructors lack `.prototype` (~1500+ tests, cascading)
**Root cause**: Built-in constructors (Boolean, Number, String, Array, Object,
Function, Error, etc.) are lightfuncs. The GETPROP lightfunc handler (`vm.c3:2213`)
resolves `.name` and `.length`, but for "prototype" it walks `Function.prototype`
which doesn't have `.prototype` → returns `undefined`.
**Cascade**: `Boolean.prototype` === `undefined` → `Object.getOwnPropertyDescriptor(
Boolean.prototype, 'toString')` → "called on non-object" — a single root cause
explains BOTH the missing `.prototype` and the getOwnPropertyDescriptor errors.
**Fix**: In lightfunc GETPROP handler, resolve `.prototype` by mapping fn_index
to the corresponding `heap.X_proto` (e.g., BUILTIN_BOOLEAN → heap.bool_proto).

### 3. Built-in method `.name` absent on lightfunc methods (~800+ tests)
**Impact**: `Boolean.prototype.toString.name` should be "toString". Lightfunc
methods don't have `.name` as an own property (GETPROP resolves it for lightfuncs
but it's not in the object's property table — so `getOwnPropertyDescriptor` returns null).
**Fix**: When registering built-in methods via `put_prop`, they should be registered
as lightfunc TVal values. But lightfuncs aren't real objects with properties.
Solution: either (a) convert lightfunc methods to real function objects, or
(b) add special handling in getOwnPropertyDescriptor for lightfunc values.

### 4. Property descriptor flags wrong on built-ins (~600+ tests)
**Impact**: Built-in methods are registered with `PROP_FLAGS_WC` (writable:true,
configurable:true) but spec requires `writable:false, configurable:true`.
`.length` on built-ins is `PROP_FLAGS_WEC` but spec requires non-writable.
**Fix**: Change `PROP_FLAGS_WC` → `PROP_FLAGS_C` for method registrations;
change `.length` from `PROP_FLAGS_WEC` → `PROP_FLAGS_C` for built-in functions.

---

## Next Tasks (ordered by impact)

### High Priority (100+ tests each)

1. ~~Set `.name` on function objects during CLOSURE~~ — ✅ DONE (Session 100).

2. **Fix lightfunc `.prototype` resolution** — Unblocks ~1500+ tests.
   `vm.c3:2213`: add `.prototype` case in lightfunc GETPROP handler.
   Map fn_index → heap's proto fields (bool_proto, number_proto, etc. —
   already exist, just not connected to lightfunc dispatch).

3. **Fix built-in method `.name` for lightfunc methods** — Unblocks ~800+ tests.
   Lightfunc methods registered via `put_prop` have no `.name` property.
   Options: convert to real function objects, or special-case lightfunc
   in `getOwnPropertyDescriptor`.

4. **Fix property descriptor flags on built-ins** — Unblocks ~600+ tests.
   Methods use `PROP_FLAGS_WC` but spec requires writable:false.
   `.length` uses `PROP_FLAGS_WEC` but spec requires writable:false.
   The constant already exists: `PROP_FLAGS_C` (writable:false, enumerable:false,
   configurable:true). Use that for methods and `.length`.

### Medium Priority

5. **Instanceof lightfunc handling** — `instanceof` walks `rval.prototype` but
   doesn't handle lightfuncs (crashes on null ptr). `vm.c3:1893`.
6. **String wrapper `.length` auto-boxing** — `typeof "hello".length` gives
   `undefined` because string primitives don't auto-box.
7. **String wrapper property enumeration** — String indices should enumerate
   via `for-in` and `Object.keys`.
8. **RegExp .constructor/.prototype chain** — regexp prototype chain wiring.
9. **JSON.stringify edge cases** — replacer/space array handling.

### Deferred

- Iterator protocol (for-of, spread, Array.from, generators)
- Async microtask scheduling (Promise tests)
- Private class fields/methods
- Nested/advanced destructuring patterns
- Reflect, Proxy

---

## Test Infrastructure

- **Runner**: `python3 scripts/run_test262.py` (multi-worker, parallel)
- **Quick test**: `bash test262_runner/quick.sh`
- **Skip list**: `scripts/test262_skip.cfg` — shared between all runners, derived from `test262_relevance_report.md`
- **Build**: `c3c build batch_test_vm` (for test262) or `c3c build test_vm` (for single files)
- **Phase counts**: `bash scripts/count_test262_by_phase.sh`

---

## Session History (condensed)

### Session 98: Fix arithmetic ToNumber coercion
BOOLEAN/OBJECT/NULL/UNDEFINED/STRING → ToNumber for all 16 operators.
Phase 2: +161 pass. Phase 5: +16 pass.

### Session 97: GETVAR/TYPEOFIDENT inline cache (VarIC)
Per-instruction IC caching env_record + prop_idx for global variable lookups.
~3% improvement on bench_loop.

### Session 96b: Array fast paths + grow_props relocation fix
Array ops bypass string-based property lookup for dense arrays.
Fixed array data corruption during property table growth.
bench_array: 16s → 49ms (411x faster).

### Session 96a: HObject merge conflict + PUTPROP fast path regression
Fixed unresolved git merge conflict in HObject struct causing VM_ERROR.
Removed `array_part != null` guard from PUTPROP fast path.
bench_array: 16s → 49ms.

### Session 95: VM dispatch optimizations
Inlined handle_return, same-function return optimization, @inline annotation.
−2-7% on recursion/function_call benchmarks.

### Session 93: Fix `typeof X.y` compiler bug
TYPEOFIDENT on member expressions now correctly emits GETPROP + TYPEOF.
Unblocks ~100+ test262 tests.

### Session 92: Optimized arguments object (exotic ARGUMENTS class)
Eliminated O(argc) string allocation per call. Arguments stored as dense array.

### Session 91: Array.prototype.flat/flatMap
flat() in C, flatMap() as compiled JS.

### Session 90: AggregateError + Promise.any
Built-in error type + Promise.any(iterable).

### Session 89: Promise.allSettled(iterable)
All-settled combinator with per-element status objects.

### Session 88: Fix Promise.prototype.catch onRejected
.catch() was dropping the handler. Phase 17-20: +75 pass.

### Session 87: Date.prototype.toLocaleString methods
Phase 8: +36 pass.

### Session 67: Fix bracket assignment lhs_mode clobbering
obj[key] = value compiled to GETPROP instead of PUTPROP. Phase 3: +813 pass.

---

## Suggested Next Task

**Fix lightfunc `.prototype` resolution** — Unblocks ~1500+ tests.
In `vm.c3` GETPROP lightfunc handler (~line 2213), add a `.prototype` case that
maps each builtin function index to its corresponding `heap.X_proto`
(e.g., BUILTIN_BOOLEAN → heap.bool_proto, BUILTIN_NUMBER → heap.number_proto,
etc.). The proto fields already exist on the heap; they just aren't connected
to lightfunc dispatch. This single fix cascades: once `Boolean.prototype` is
an object instead of `undefined`, `Object.getOwnPropertyDescriptor(Boolean.prototype, 'toString')`
starts working too, resolving the category #4 cascade.